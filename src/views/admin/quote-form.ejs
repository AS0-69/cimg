<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #f5f7fa; }
        .header { background: #009972; color: white; padding: 1rem 2rem; display: flex; justify-content: space-between; align-items: center; }
        .header h1 { font-size: 1.5rem; }
        .nav { display: flex; gap: 2rem; }
        .nav a { color: white; text-decoration: none; padding: 0.5rem 1rem; border-radius: 4px; transition: background 0.2s; }
        .nav a:hover, .nav a.active { background: rgba(255,255,255,0.2); }
        .container { max-width: 1200px; margin: 2rem auto; padding: 0 2rem; }
        .form-card { background: white; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); padding: 2rem; }
        .form-group { margin-bottom: 1.5rem; }
        .form-group label { display: block; margin-bottom: 0.5rem; font-weight: 600; color: #2d3748; }
        .form-group input, .form-group select, .form-group textarea { width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px; font-size: 1rem; transition: border 0.2s; }
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus { outline: none; border-color: #009972; }
        .form-group textarea { resize: vertical; min-height: 120px; }
        .conditional-field { display: none; margin-top: 0.5rem; }
        .conditional-field.show { display: block; }
        .checkbox-group { display: flex; align-items: center; gap: 0.5rem; }
        .checkbox-group input[type="checkbox"] { width: auto; }
        .btn { padding: 0.75rem 2rem; border: none; border-radius: 8px; font-size: 1rem; font-weight: 600; cursor: pointer; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem; text-decoration: none; }
        .btn-primary { background: #009972; color: white; }
        .btn-primary:hover { background: #007d5e; transform: translateY(-1px); }
        .btn-secondary { background: #e2e8f0; color: #4a5568; }
        .btn-secondary:hover { background: #cbd5e0; }
        .form-actions { display: flex; gap: 1rem; margin-top: 2rem; }
        .required { color: #e53e3e; }
    </style>
</head>
<body>
    <div class="header">
        <h1><i class="fas fa-quote-right"></i> <%= quote ? 'Modifier' : 'Nouvelle' %> citation</h1>
        <div class="nav">
            <a href="/admin/dashboard">Dashboard</a>
            <a href="/admin/quotes" class="active">Citations</a>
            <a href="/">Retour au site</a>
        </div>
    </div>

    <div class="container">
        <div class="form-card">
            <form method="POST" action="<%= quote ? `/admin/quotes/${quote.id}` : '/admin/quotes' %>">
                
                <div class="form-group">
                    <label>Texte (Français) <span class="required">*</span></label>
                    <textarea name="text_fr" required><%= quote ? quote.text_fr : '' %></textarea>
                </div>

                <div class="form-group">
                    <label>Texte (Turc) <span class="required">*</span></label>
                    <textarea name="text_tr" required><%= quote ? quote.text_tr : '' %></textarea>
                </div>

                <div class="form-group">
                    <label>Auteur <span class="required">*</span></label>
                    <select name="author" id="author" required>
                        <option value="">-- Sélectionner --</option>
                        <% authors.forEach(author => { %>
                            <option value="<%= author.name %>" <%= quote && quote.author === author.name ? 'selected' : '' %>>
                                <%= author.name %>
                            </option>
                        <% }); %>
                        <option value="autre">➕ Autre (nouvel auteur)</option>
                    </select>
                    <div class="conditional-field" id="new_author_field">
                        <input type="text" name="new_author" placeholder="Nom du nouvel auteur (ex: Nom (RA))">
                    </div>
                </div>

                <div class="form-group">
                    <div class="checkbox-group">
                        <input type="checkbox" id="active" name="active" <%= quote && quote.active ? 'checked' : '' %>>
                        <label for="active" style="margin-bottom: 0;">Afficher sur le site</label>
                    </div>
    
    <script>
        document.getElementById('author').addEventListener('change', function() {
            document.getElementById('new_author_field').classList.toggle('show', this.value === 'autre');
        });
    </script>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Enregistrer
                    </button>
                    <a href="/admin/quotes" class="btn btn-secondary">Annuler</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
