<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #f5f7fa; }
        .header { background: #009972; color: white; padding: 1rem 2rem; display: flex; justify-content: space-between; align-items: center; }
        .header h1 { font-size: 1.5rem; }
        .nav { display: flex; gap: 2rem; }
        .nav a { color: white; text-decoration: none; padding: 0.5rem 1rem; border-radius: 4px; transition: background 0.2s; }
        .nav a:hover, .nav a.active { background: rgba(255,255,255,0.2); }
        .container { max-width: 1200px; margin: 2rem auto; padding: 0 2rem; }
        .form-card { background: white; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); padding: 2rem; }
        .form-group { margin-bottom: 1.5rem; }
        .form-group label { display: block; margin-bottom: 0.5rem; font-weight: 600; color: #2d3748; }
        .form-group input, .form-group textarea { width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px; font-size: 1rem; transition: border 0.2s; }
        .form-group input:focus, .form-group textarea:focus { outline: none; border-color: #009972; }
        .form-group textarea { resize: vertical; min-height: 120px; }
        .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
        .form-row-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem; }
        .checkbox-group { display: flex; align-items: center; gap: 0.5rem; }
        .checkbox-group input[type="checkbox"] { width: auto; }
        .image-upload-area { border: 2px dashed #cbd5e0; border-radius: 8px; padding: 2rem; text-align: center; background: #f7fafc; cursor: pointer; transition: all 0.2s; }
        .image-upload-area:hover { border-color: #009972; background: #e6f7f2; }
        .image-preview { margin-top: 1rem; text-align: center; }
        .image-preview img { max-width: 300px; border-radius: 8px; }
        .btn { padding: 0.75rem 2rem; border: none; border-radius: 8px; font-size: 1rem; font-weight: 600; cursor: pointer; transition: all 0.2s; display: inline-flex; align-items: center; gap: 0.5rem; text-decoration: none; }
        .btn-primary { background: #009972; color: white; }
        .btn-primary:hover { background: #007d5e; transform: translateY(-1px); }
        .btn-secondary { background: #e2e8f0; color: #4a5568; }
        .btn-secondary:hover { background: #cbd5e0; }
        .form-actions { display: flex; gap: 1rem; margin-top: 2rem; }
        .required { color: #e53e3e; }
    </style>
</head>
<body>
    <div class="header">
        <h1><i class="fas fa-hand-holding-heart"></i> <%= donation ? 'Modifier' : 'Nouvelle' %> campagne</h1>
        <div class="nav">
            <a href="/admin/dashboard">Dashboard</a>
            <a href="/admin/donations" class="active">Dons</a>
            <a href="/">Retour au site</a>
        </div>
    </div>

    <div class="container">
        <div class="form-card">
            <form method="POST" action="<%= donation ? `/admin/donations/${donation.id}` : '/admin/donations' %>" enctype="multipart/form-data">
                
                <div class="form-group">
                    <label>Titre de la campagne <span class="required">*</span></label>
                    <input type="text" name="title" value="<%= donation ? donation.title : '' %>" required>
                </div>

                <div class="form-group">
                    <label>Description <span class="required">*</span></label>
                    <textarea name="description" required><%= donation ? donation.description : '' %></textarea>
                </div>

                <div class="form-row-3">
                    <div class="form-group">
                        <label>Objectif (€) <span class="required">*</span></label>
                        <input type="number" name="goal_amount" value="<%= donation ? donation.goal_amount : '' %>" step="0.01" min="0" required>
                    </div>
                    <div class="form-group">
                        <label>Montant collecté (€)</label>
                        <input type="number" name="current_amount" value="<%= donation ? donation.current_amount : 0 %>" step="0.01" min="0">
                    </div>
                    <div class="form-group">
                        <label>Date de fin</label>
                        <input type="date" name="end_date" value="<%= donation && donation.end_date ? new Date(donation.end_date).toISOString().split('T')[0] : '' %>">
                    </div>
                </div>

                <div class="form-group">
                    <div class="checkbox-group">
                        <input type="checkbox" id="active" name="active" <%= donation && donation.active ? 'checked' : '' %>>
                        <label for="active" style="margin-bottom: 0;">Campagne active (afficher sur le site)</label>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Enregistrer
                    </button>
                    <a href="/admin/donations" class="btn btn-secondary">Annuler</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
