<%- include('../partials/admin-form-head') %>

    <div class="container">
        <div class="form-card">
            <form method="POST" action="<%= member ? `/admin/members/${member.id}` : '/admin/members' %>" enctype="multipart/form-data">
                
                <div class="form-row">
                    <div class="form-group">
                        <label>Prénom <span class="required">*</span></label>
                        <input type="text" name="first_name" id="first_name" value="<%= member ? member.first_name : '' %>" required maxlength="50" data-maxlength="50">
                    </div>
                    <div class="form-group">
                        <label>Nom <span class="required">*</span></label>
                        <input type="text" name="last_name" id="last_name" value="<%= member ? member.last_name : '' %>" required maxlength="50" data-maxlength="50">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Pôle <span class="required">*</span></label>
                        <select name="pole" id="pole" required>
                            <option value="">-- Sélectionner --</option>
                            <option value="AT" <%= member && member.pole === 'AT' ? 'selected' : '' %>>AT - Ana Teşkilat (Hommes adultes)</option>
                            <option value="GT" <%= member && member.pole === 'GT' ? 'selected' : '' %>>GT - Gençlik Teşkilatı (Jeunes hommes)</option>
                            <option value="KT" <%= member && member.pole === 'KT' ? 'selected' : '' %>>KT - Kadın Teşkilatı (Femmes adultes)</option>
                            <option value="KGT" <%= member && member.pole === 'KGT' ? 'selected' : '' %>>KGT - Kadın Gençlik Teşkilatı (Jeunes femmes)</option>
                            <option value="autre">➕ Autre</option>
                        </select>
                        <div class="conditional-field" id="new_pole_field">
                            <input type="text" name="new_pole" placeholder="Nom du nouveau pôle">
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Fonction <span class="required">*</span></label>
                        <select name="role" id="role" required>
                            <option value="">-- Sélectionner --</option>
                            <% roles.forEach(role => { %>
                                <option value="<%= role.name %>" <%= member && member.role === role.name ? 'selected' : '' %>>
                                    <%= role.name %>
                                </option>
                            <% }); %>
                            <option value="autre">➕ Autre (nouvelle fonction)</option>
                        </select>
                        <div class="conditional-field" id="new_role_field">
                            <input type="text" name="new_role" placeholder="Nom de la nouvelle fonction">
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label>Ordre d'affichage</label>
                    <input type="number" name="order" value="<%= member ? member.order : 0 %>" min="0">
                </div>

                <div class="form-group">
                    <label>Photo</label>
                    <div class="image-upload-area" id="uploadArea">
                        <i class="fas fa-user-circle icon-lg icon-primary"></i>
                        <p><strong>Cliquez pour téléverser une photo</strong></p>
                    </div>
                    <input type="file" name="image" id="image" accept="image/*" class="hidden-input">
                    
                    <div class="image-preview" id="preview">
                        <% if (member && member.image) { %>
                            <img src="<%= member.image %>" alt="Photo actuelle">
                        <% } %>
                    </div>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Enregistrer
                    </button>
                    <a href="/admin/members" class="btn btn-secondary">Annuler</a>
                </div>
            </form>
        </div>
    </div>

    <%- include('../partials/admin-form-scripts') %>
</body>
</html>
